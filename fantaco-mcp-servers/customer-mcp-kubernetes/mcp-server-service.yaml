apiVersion: v1
kind: Service
metadata:
  name: mcp-customer-service
  labels:
    app: mcp-customer
spec:
  type: ClusterIP
  selector:
    app: mcp-customer
  ports:
  - port: 9001
    targetPort: 9001
    protocol: TCP
    name: http
