apiVersion: v1
kind: Service
metadata:
  name: mcp-finance-service
  labels:
    app: mcp-finance
spec:
  type: ClusterIP
  selector:
    app: mcp-finance
  ports:
  - port: 9002
    targetPort: 9002
    protocol: TCP
    name: http
